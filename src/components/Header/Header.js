import React from 'react';
import NavMenu from './NavMenu';
import TopNav from './TopNav';

const Header = () => {
    return (
        <div>
            <TopNav />
            <NavMenu />
        </div>
    );
};

export default Header;